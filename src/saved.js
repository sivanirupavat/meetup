import React, { Component } from 'react'

export class Saved extends Component {
    render() {
        return (
            <div>
                saved page
            </div>
        )
    }
}

export default Saved;
